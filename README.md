# 🤖 Tron Energy Stasher Bot (Telegram)

Автоматический бот для сети **TRON**, предназначенный для управления ресурсом **Energy**.  
Бот позволяет оперативно **делегировать ("спрятать")** максимально возможное количество **TRX** с **"Адреса-Котлеты"** на **"Адрес-Тайник"** для получения **Energy**, а затем **возвращать ("отзывать")** их, когда ресурс не нужен.  
Также реализована функция **отложенного (запланированного)** делегирования и возврата.

---

## ✨ Основные возможности

- **Скрытие:** Делегирование максимального доступного количества **TRX** с главного кошелька (`MAIN_WALLET`) на целевой (`STASHING_TARGET`) для получения **ENERGY**.  
- **Возврат:** Отзыв ранее делегированных **TRX** обратно на главный кошелек.  
- **Отложенное выполнение:** Планирование делегирования и последующего возврата на заданное время и длительность.  
- **Управление задачами:** Просмотр и удаление активных отложенных задач.  
- **Логирование:** Отправка уведомлений об успешных операциях и критических ошибках администраторам Telegram.  
- **Логика обьединения:** Если есть близкостоящие операции скрытия, или накладывающиеся операции сrрытия, они склеиваются в одну задачу для экономии газа.
- **Сохранение статуса работы:** Бот сохраняет статус автослежения, при жестком перезапуске контейнера/программы.
---

## ⚙️ Установка и Настройка

### 1. Требования

- **Python 3.8+**  
- **Docker** (рекомендуется для простоты развертывания)  
- Активный кошелек **TRON** (`MAIN_WALLET`) с застейканым **TRX** в энергию для делегирования, и управляющим приватным ключом для использования Multi-signature подписи.

---

### 2. Файл `.env`

Создайте в корне проекта файл `.env` и заполните его переменными:

```env
API_KEY_TRONSCAN="АПИ_КЛЮЧ_КАБИНЕТА_ТРОНСКАН" # апи ключ в тронскане

API_KEY_TRONGRID="АПИ_КЛЮЧ_ТРОНГРИД" # апи ключ тронгрида

PRIV_KEY_MY_HEX="ВАШ_ПРИВАТНЫЙ_КЛЮЧ"

PERM_ID=9 # порядковый номер пермишена на адресе MAIN_WALLET

MAIN_WALLET="ТРОН_АДРЕС_КОШЕЛЬКА_С_ЭНЕРГИЕЙ" # кошелек чью энергию прячем

STASHING_TARGET="ТРОН_АДРЕС_КОШЕЛЬКА_КУДА_ПРЯЧЕМ_ЭЕНЕРГИЮ" # кошелек куда прячем

API_TOKEN="КЛЮЧ_БОТА_В_ТЕЛЕГРАМ"

ADMIN_IDS="ID_АДМИНА_БОТА"

zone_time = 3 # сдвиг времени к серверному (+3)

CHECK_INTERVAL_MINUTES = 10 # как часто проверяем на входящие делегации
TIME_BUY_ENERGY = 58 # через сколько минут от  входящей аренды, делаем скрытие энергии
AUTO_HOLD_MINUTES = 5 # на сколько минут прячем автоматически
SLICE_MINUTES = 5 # Время "склейки" между задачами
```
---

## 🚀 Раздел: Запуск проекта

### 3. Запуск через Docker (Рекомендуется)

Использование Docker обеспечивает изолированную и надежную среду.

```bash
# Сборка образа (использует ваш Dockerfile)
docker build -t tron-stasher-bot .

# Запуск контейнера в фоновом режиме (-d). 
# --restart unless-stopped: автоматический перезапуск после сбоев.
# --env-file ./.env: загружает все переменные окружения из вашего файла .env.
docker run -d \
  --restart unless-stopped \
  --name tron_stasher_instance \
  --env-file ./.env \
  tron-stasher-bot

