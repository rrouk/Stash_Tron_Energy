# 🤖 Tron Energy Stasher Bot (Telegram)

Автоматический бот для сети **TRON**, предназначенный для управления ресурсом **Energy**.  
Бот позволяет оперативно **делегировать ("спрятать")** максимально возможное количество **TRX** с **"Адреса-Котлеты"** на **"Адрес-Тайник"** для получения **Energy**, а затем **возвращать ("отзывать")** их, когда ресурс не нужен.  
Также реализована функция **отложенного (запланированного)** делегирования и возврата.

---

## ✨ Основные возможности

- **Скрытие ($\text{Stash}$):** Делегирование максимального доступного количества **TRX** с главного кошелька (`MAIN_WALLET`) на целевой (`STASHING_TARGET`) для получения **ENERGY**.  
- **Возврат ($\text{Reclaim}$):** Отзыв ранее делегированных **TRX** обратно на главный кошелек.  
- **Отложенное выполнение ($\text{Scheduler}$):** Планирование делегирования и последующего возврата на заданное время и длительность.  
- **Управление задачами:** Просмотр и удаление активных отложенных задач.  
- **Логирование:** Отправка уведомлений об успешных операциях и критических ошибках администраторам Telegram.  

---

## ⚙️ Установка и Настройка

### 1. Требования

- **Python 3.8+**  
- **Docker** (рекомендуется для простоты развертывания)  
- Активный кошелек **TRON** (`MAIN_WALLET`) с **TRX** для делегирования и токеном **Permission ID** (для использования Multi-signature или Permission-аккаунта).

---

### 2. Файл `.env`

Создайте в корне проекта файл `.env` и заполните его переменными:

```env
# --- Telegram ---
API_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
ADMIN_IDS=12345678,90123456  # ID администраторов через запятую
zone_time=3                  # Часовой пояс (например, 3 для UTC+3/МСК)

# --- TRON API Keys ---
API_KEY_TRONGRID=YOUR_TRONGRID_API_KEY
API_KEY_TRONSCAN=YOUR_TRONSCAN_API_KEY

# --- TRON Wallets & Private Key ---
PRIV_KEY_MY_HEX=YOUR_PRIVATE_KEY_HEX  # ВАЖНО: храните ключ в безопасности!
MAIN_WALLET=TRON_MAIN_ADDRESS         # Адрес-Котлета
STASHING_TARGET=TRON_TARGET_ADDRESS   # Адрес-Тайник

# Permission ID (для Multi-signature)
PERM_ID=2  # Установите 0 или нужный ID
```
---

## 🚀 Раздел: Запуск проекта

### 3. Запуск через Docker (Рекомендуется)

Использование Docker обеспечивает изолированную и надежную среду.

```bash
# Сборка образа (использует ваш Dockerfile)
docker build -t tron-stasher-bot .

# Запуск контейнера в фоновом режиме (-d). 
# --restart unless-stopped: автоматический перезапуск после сбоев.
# --env-file ./.env: загружает все переменные окружения из вашего файла .env.
docker run -d \
  --restart unless-stopped \
  --name tron_stasher_instance \
  --env-file ./.env \
  tron-stasher-bot

